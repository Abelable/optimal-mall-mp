<view class="nav-bar {{ navBarBgVisible ? 'show-bg' : '' }}" style="padding-top: {{statusBarHeight}}px;">
  <view class="nav-bar-inner">
    <back-icon />
    <view class="nav-bar-title">乡村振兴推广员</view>
  </view>
</view>

<view class="header" style="padding-top: {{statusBarHeight + 44}}px;" />

<view class="title">认养专区</view>
<scroll-view class="livestock-list" scroll-x enable-flex>
  <livestock-item wx:for="{{livestockList}}" wx:key="index" item="{{item}}" />
</scroll-view>

<view class="title">礼包专区</view>
<view class="goods-list">
  <fall-flow list="{{giftList}}" generic:item="goods-item" />
  <empty-illus wx:if="{{!giftList.length}}" desc="暂无商品" />
  <view class="no-more-tips" wx:if="{{giftList.length}}">～没有更多了～</view>
</view>
